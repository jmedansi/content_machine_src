[pytest]
testpaths = tests
norecursedirs = .venv .agents node_modules __pycache__ .archive scratch machines/*/scratch
asyncio_mode = auto
markers =
    integration: tests requiring a live dashboard server on 127.0.0.1:8000
addopts = -p no:cacheprovider -m "not integration"